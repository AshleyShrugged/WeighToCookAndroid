<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.weightocook.weightocook" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/teamd_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >


        <activity android:name="Splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".SearchResultsActivity"
            android:label="@string/title_activity_search_results" >
        </activity>
        <activity
            android:name=".DinnerCategoryActivity"
            android:label="@string/title_activity_dinner_category" >
        </activity>
        <activity
            android:name=".DessertsCategoryActivity"
            android:label="@string/title_activity_desserts_category" >
        </activity>
        <activity
            android:name=".BakedTeriyakiChickenActivity"
            android:label="@string/title_activity_baked_teriyaki_chicken" >
        </activity>
    </application>

</manifest>
